## Responsive Design Rule
Always ensure that all CSS changes are mobile-first or properly account for responsive breakpoints using @media queries. Never implement a feature that breaks on mobile screens.
